import { ArticleLayout } from '@/components/ArticleLayout';
import { PlayNav } from '@/components';

export const meta = {
  author: 'Matt Mischewski',
  order: 4,
  date: '2023-02-12',
  title: 'Defence',
  description: "You won't lose if the opposition cannot score.",
};

export default (props) => <ArticleLayout meta={meta} {...props} />;

Touch a person before they can put the ball over the try line.

## How

- Get back onside
- Don't watch the ball
- Stay aligned
- Be vocal

<PlayNav />
